
  <div class="games_available col-10">
    <h3>Available Games</h3>
    <ul id="games_available" >
      <% Game.available.each do |g| %>
        <li>Game: <%= link_to g.id, game_path(game) %> </li>
      <% end %>
    </ul> 
  </div>